export interface Product {
  productId: number;
  productColor: string;
  productSecondColor: string;
  productSize: string;
  productSecondSize: string;
  productName: string;
  productDescription: string;
  productPrice: number;
}
